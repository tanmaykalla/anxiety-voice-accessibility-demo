{
  "name": "anxiety-voice-accessibility-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Vocal Bridge voice accessibility demo for Adventures With Anxiety",
  "type": "commonjs",
  "scripts": {
    "build": "esbuild src/vocalbridge-stt.entry.js --bundle --format=iife --global-name=VoiceRPGVocalBridgeBundle --outfile=vocalbridge-stt.js",
    "localize:hi:sync": "node tools/sync-localization-source.mjs",
    "localize:hi:translate": "node --env-file=.env node_modules/voice-accessibility-sdk/node/localize.mjs translate localization/game.en-US.json localization/catalog.hi-IN.json --provider gemini --model gemini-3.5-flash --bible localization/locale-bible.hi-IN.json --batch-size 20 --delay-ms 5000",
    "localize:hi:build": "node tools/apply-localization.mjs --allow-drafts",
    "localize:hi:validate": "node tools/validate-localization.mjs",
    "localize:hi": "npm run localize:hi:sync && npm run localize:hi:translate && npm run localize:hi:build && npm run localize:hi:validate",
    "start": "npm run build && node --env-file=.env server.mjs",
    "test": "node --test test/*.test.cjs",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "@vocalbridgeai/sdk": "^0.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "voice-accessibility-sdk": "github:tanmaykalla/voice-rpg#99c9abf"
  },
  "engines": {
    "node": ">=20"
  }
}
